const EmptyEditor = () => {
  return (
    <div className="flex h-[300px] w-1/2 items-center justify-center rounded-lg border border-dashed border-orange-500">
      <p className="text-4xl text-white">
        Select two files to start the comparation
      </p>
    </div>
  );
};

export default EmptyEditor;
